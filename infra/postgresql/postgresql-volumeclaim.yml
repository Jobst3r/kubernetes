apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-volume-claim
  namespace: jobster
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5G
  storageClassName: manual
